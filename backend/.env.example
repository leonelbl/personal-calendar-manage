# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================
# This file serves as a template for the required environment variables.
# Copy this file to .env and fill in the actual values.

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
# Development: Use the same credentials as in docker-compose.yml
# Production: Use your production database credentials
DATABASE_URL="postgresql://booking_user:booking_pass@localhost:5432/booking_db?schema=public"

# ============================================
# Server Configuration
# ============================================
# Port where the backend server will run
PORT=3001

# Environment mode (development, production, test)
NODE_ENV=development

# ============================================
# JWT Authentication
# ============================================
# Secret key for signing JWT tokens
# IMPORTANT: Generate a strong random string (minimum 64 characters)
# You can generate one with: openssl rand -base64 64
JWT_SECRET="your-super-secure-jwt-secret-change-in-production-min-64-chars"

# ============================================
# Google OAuth 2.0
# ============================================
# Get these credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Add authorized redirect URIs: http://localhost:3001/auth/google/callback
# 3. Enable Google Calendar API

# Google OAuth Client ID
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"

# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Google OAuth Callback URL
# Development: http://localhost:3001/auth/google/callback
# Production: https://your-domain.com/auth/google/callback
GOOGLE_CALLBACK_URL="http://localhost:3001/auth/google/callback"

# ============================================
# Frontend Configuration
# ============================================
# Frontend URL for CORS configuration and redirects after authentication
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
FRONTEND_URL="http://localhost:3000"